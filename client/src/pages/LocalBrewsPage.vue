<template>
  <section class="rounded-3xl border border-dashed border-slate-300 bg-white px-6 py-12 shadow-panel">
    <div class="mx-auto max-w-2xl space-y-4 text-center">
      <p class="text-sm font-semibold uppercase tracking-[0.25em] text-brand-600">Starter Route</p>
      <h2 class="text-3xl font-bold text-slate-900">Local Brews</h2>
      <p class="text-sm text-slate-600 sm:text-base">
        This page is intentionally left as a minimal stub so you can practice building the city-based `filter` query flow yourself.
      </p>
      <p class="text-sm text-slate-500">
        Use the `Home` page as your example for Apollo queries, generated `graphql()` documents, responsive layout, and card-grid reuse.
      </p>
    </div>
  </section>
</template>
